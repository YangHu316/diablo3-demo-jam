# Godot
.godot/
*.tmp
*.log

# Import cache
.import/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
*.swp
*.swo

# AI assistant local settings
.claude/
.codebuddy/
.mcp.json

# MCP / dev-only addons (per-user, not part of the game)
addons/godot_mcp/

# Export builds
export/
*.pck
*.zip

# 个人本地配置覆盖 (Godot override.cfg) — 不推到 origin,只在本机生效
override.cfg

# AI 关卡管线的俯拍/重叠比对输出 (每次运行重新生成,不入库)
tools/_capture/
