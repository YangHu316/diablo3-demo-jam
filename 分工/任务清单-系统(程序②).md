# 任务清单 · 系统(程序②)

> 你的域:掉落/物品/词缀、背包/装备、升级、梦魇倍率、数据表加载、对话系统、**新手引导框架**、**小地图逻辑**、**关卡↔Boss房传送门(场景切换)**、UI 逻辑(HUD/背包/Tooltip)、检查点存档、集成出包。**经济(金币/修理)本 Demo 不做。**
> 引擎 Godot 4 / GDScript。勾选用 `[x]`。
> **标记**:🆕 本次新增(2026-06-13) / 🔧 本次修改 / ❌ 本次取消 / ✅ 已完成 / ⏳ 待做。

## 协作接口(谁等你 / 你等谁)
- **你等 关卡C**:数值(技能/怪物/词缀/掉落/XP)。Day1 上午先用占位数值,数值定稿后换表即可。
- **你给 战斗①**:数据表加载器 API、升级信号(`level_up`)。**这是 Day1 第一优先**,战斗①在等。
- **你给 角色D**:UI 逻辑节点结构(HUD/背包/Tooltip 待皮)、掉落品质信号(`loot_dropped(quality)` 给光柱/音效挂)、各界面流程。
- **你给 关卡C**:对话框系统(接对话内容)、掉落配置入口。
- **Hour 0 必须对齐**:数据表 schema(.tres/CSV)、autoload 结构、命名规范、对外信号命名。

## Hour 0(主导)
- [x] 让 Claude 出 Godot 项目目录骨架 + autoload 结构 + 数据表 schema,定下工程地基 — ✅ 完成
- [x] 和战斗①敲定:数值表字段 + 升级信号;和角色D敲定:UI 节点约定 + 掉落品质信号名 — ✅ 完成

## DAY 1 任务
- [x] 数据表加载器(技能/怪物/物品数值从 .tres/CSV 读)`(2h)` — ✅ `DataTables` autoload + 查询 API,战斗①已对接
- [x] 物品/词缀系统:按品质 + 词缀池生成物品,物品数据模型 `(3h)` — ✅ `ItemGenerator` 实测通过
- [x] 掉落与拾取:击杀掉落、地面名牌、拾取入包 `(2h)` — ✅ 全链路 headless + 连场景可玩
- [x] 背包 + 装备:网格背包、装备槽、装备生效改属性 `(2.5h)` — ✅ 40 格/13 槽/属性聚合 + B 键 UI
- [x] XP/升级:击杀→XP→升级→属性成长 + 技能解锁钩子(发 `level_up` 信号)`(1.5h)` — ✅ 完成
- [x] **超额**:5 档稀有度 + 基底/史诗物品表 + 五档程序化命名 — ✅(详见进展 §6.5/§6.6)

### Day1 EOD 你负责通过的部分
掉落 → 捡起 → 入包 → 换装 → 属性变化;升级 → 属性涨 + 发信号给战斗①解锁技能。

## DAY 2 任务
- [ ] 对比 Tooltip:悬停时并排「已装备 vs 这件」,伤害/生存两行增减箭头(绿↑红↓)`(2.5h)`
- [ ] 传奇特效(4~5 件 on-effect)+ 智能掉落(职业定向)+ 首传奇保底(~10min)+ Boss 必掉 `(3h)`
- [ ] 🆕 **新增｜新手引导框架**:Toast(右下,8s 淡出)+ 高亮(目标 UI 金框)两档,**触发 / 完成判定 / 超时兜底** 三字段,配置表驱动(接关卡C的 0~8min 引导脚本文案)`(2h)` — 完成判定:走/射击/拾取/穿戴/换技能 各触发一次且自发完成可静默跳过
- [ ] 🆕 **新增｜小地图**:小地图框 + 玩家/目标箭头 + **传奇橙星标 + 目标指引光圈**(接 `loot_dropped` 信号挂星标)`(1h)` — 配角色D小地图视觉
- [ ] 🆕 **新增｜关卡↔Boss 房传送门**:关底传送门交互 → 场景切换/加载 Boss 房(接关卡C触发点 + 角色D传送门 VFX)`(1h)` — 完成判定:踩传送门进 Boss 房
- [ ] 🔧 **改｜梦魇模式**:重进流程、怪物 ×倍率、掉率加成 + 进层横幅、击杀 Boss 后解锁入口;**复用主线大关卡 Tile(零新增地图)** `(2.5h)`
- [ ] **对话框系统**:NPC 对话 UI(逐句推进,接关卡C的文案)`(1h)`
- [ ] 检查点/复活 + 通关页 + 结算页流程 + 存运行状态 `(1.5h)`
- [ ] 修 bug 缓冲 `(0.5h)`

## 🆕 V3.0 单局大秘境改造 · Day2 追加(2026-06-14,关卡C 分配)

> 来源:`数值表/大秘境-单局固定数值与关卡配置.md` + `boss_drop_list.csv`;方向案 `暗黑破坏神3-Demo策划案/10-方向大改-单局大秘境(形似优先).md`。**仅 L2;小怪零掉;守门人固定爆装橙+绿;严禁其他品质。**

- [ ] **掉落机制重构(核心)**:关闭 TRASH/ELITE_BLUE/CHAMPION_YELLOW/CHEST 所有掉落源(`drop_system.gd` SOURCE_CONFIG 置零/短路);**仅守门人死触发固定爆装** —— 读 `数值表/boss_drop_list.csv`,在守门人死亡点一次性 spawn 全部 14 件(12 橙 + 2 绿)`(2h)`
- [ ] **仅橙/绿品质 + 绿套装光柱**:DropSystem 本版只产 `LEGENDARY`(橙);新增 `ItemInstance.is_set:bool` + `loot_drop._apply_visuals` 绿光柱分支(`is_set→绿`,绿优先级 > 橙);`boss_drop_list` 13/14 号标 `is_set=true` `(1h)` — 对齐 10 号 §6.6
- [ ] **读新 CSV**:`gen_data_tables.gd` + `DataTables` 加载 `boss_drop_list.csv`(最低限度);可选 `rift_monsters/player_loadout`。新 CSV `.import` 已设 keep `(1h)`
- [ ] **移除小关卡 L1**:场景流程去掉 `level_01`,启动直进 `level_02_play`;删/隐藏 L1 入口与切换 `(0.5h)`
- [ ] **固定装备面板(展示用)**:背包/装备面板数字写死中期档(`player_loadout.csv`);换装不改实际战斗数值 `(1h)`
- [ ] (接 10 号·P1)**RiftManager**:时间条 + 进度条(读怪 `drop_source` 权重)+ 进度满触发守门人房切场景 + 守门人死结算面板 `(2.5h)` — 与原 Day2 传送门/梦魇项取舍合并
- [ ] 🆕 **女武神之冠 → 剑舞之冠(传奇 retheme)**:`gen_data_tables.gd` 的 `valkyrie_crown` 改名「剑舞之冠」+ 效果「剑刃风暴额外多抛 1 轮剑刃且范围 +20%」(对齐 `legendaries.csv`),重跑出 `data/legendaries.tres` `(0.3h)`
- [ ] 🆕 **清理弃用 .tres(数值表已删对应 CSV)**:V3.0 已不驱动玩法的 `monsters/xp_curve/tier_table/base_items/epic_items.tres` 生成(`gen_data_tables.gd` 硬编码故当前不报错)与 `DataTables` 加载可一并移除;`ItemGenerator` 随机生成路径在固定掉落下停用 `(0.5h)`
- [ ] 🆕 (P1·接 10 号 §四/§六)**形似展示 UI 套件**:战争迷雾(`FogState`)+ 小地图 + Tab 半透明全图 + 选层界面 + 物品 hover tooltip + 时间球/功能塔逻辑——**展示用无功能**,详见 `暗黑破坏神3-Demo策划案/10-方向大改-单局大秘境(形似优先).md` 各端交接 `(按 10 号 P0/P1 排期)`
- [ ] ❌ **本版砍**:梦魇模式、概率掉落保底/智能定向、对比 Tooltip(无换装需求)、XP/升级展示

### 收尾(你负责)
- [ ] 主导集成出包(每天至少一个可玩集成包)；最终 build + 写 readme
- [ ] 功能冻结后只修系统致命 bug

## 你的砍量 / 加量项
**落后先砍**:对比 Tooltip → 只显示新旧 DPS 两个数字;传奇 5→3;梦魇 → 只做「最后一区 ×倍率 + 掉率横幅」;**新手引导 → 只留 移动/射击/首传奇 3 条 Toast**;**小地图 → 只留橙星标(砍目标光圈)**;传送门 → 同场景隔墙开门(砍跨场景加载);NPC 对话 → 压成一句字幕。
**领先再加**:梦魇 2~3 层、资源条 juice、符文系统、更多传奇。
**❌ 已砍除**:经济系统(金币/修理),不做。

## Claude 用法速查(让它主导生成,你负责接节点+集成)
让 Claude 直接产**整段可编译 GDScript + 数据资源(.tres/CSV)初版**:
- 数据表加载器、物品/词缀生成器、背包+装备、XP/升级、对比 Tooltip 逻辑
- 传奇特效钩子、智能掉落 + 保底、梦魇倍率逻辑、对话框系统、存档/检查点、通关/结算页流程
- 数据表初版:词缀池、传奇表、怪物数值、XP 曲线(让它生成,人只审)
技巧:① 让它先定**对外信号契约**(如 `loot_dropped(quality)`、`item_equipped(slot)`),角色D挂特效、战斗①接升级都靠这些;② 要「这个脚本挂哪个节点、依赖什么」;③ 报错贴全栈 + Godot 版本。
