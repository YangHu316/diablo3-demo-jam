# 战斗(程序①) · 工作进展沉淀

> 维护者:程序① / 引擎 Godot 4 / GDScript
> 最近更新:2026-06-13(5 主动技能数值按 数值表/skills_active.csv 校准)

本文档持续记录战斗域的实现进展、对外接口契约、待办与队友对接事项。**队友(系统②/角色D/关卡C)对接接口前先看本文档。**

---

## 一、当前进度总览

| 任务 | 状态 | 说明 |
|------|------|------|
| 玩家控制 (WASD + 鼠标朝向 + 2.5D 相机) | ✅ 完成 | `player.gd`,SPEED=7,翻滚已接入 |
| 核心战斗 (箭矢生成/飞行/命中/伤害) | ✅ 完成 | `arrow.gd`,支持穿透/AOE/状态注入 |
| **Juice 第一遍 (闪白/飘字/屏震/击退/僵直)** | ✅ **超额完成** | 把 Day2 的击退/僵直/玩家受击反馈一起做了 |
| 敌人 AI (近战追击 NavMesh + 成组刷怪器) | ✅ 完成 | `enemy_base.gd` 5态状态机 + `SpawnManager` autoload |
| 技能骨架 (5 槽 / CD / Focus 资源) | ✅ 完成 | `SkillSlotManager` + `SkillExecutor` + `FocusResource` autoload |
| 5 个主动技能(策划要 4,我们做了 5 全填) | ✅ 完成 | 利箭 / 多重 / 冰冻箭 / 翻滚 / 女武神 |
| Day1 EOD 验收"一波怪追上来打你" | ✅ 完成 | 进区域→5 僵尸 surround→AI 追击+造伤+被打闪白屏震 |
| 接系统②的 level_up 信号 | ✅ 完成 | `SkillSlotManager._on_level_up` 监听 ProgressionManager,据 `unlocked` id 解锁对应槽 |
| **圣堂武士随从** | ✅ 完成 | `templar.gd`:跟随 + 自动近战 + 10s 治疗 10% maxHP |
| **屠夫 Boss(普攻 + 冲锋撞墙 + 烧地板 + P1/P2 + 横扫)** | ✅ 完成 | 6 态机 IDLE/CHASE/ATTACK/SWEEP/CHARGE/ROAR/DEATH;P1 每 3 普攻 1 横扫,P2 每 2 普攻 1 横扫 |
| **简版 HUD**(血/蓝/CD/XP/死亡演出) | ✅ 完成(占位) | `scripts/ui/hud.gd` 程序化 Control;美术 D 后期重做 |
| **熔火精英词缀** | ✅ 完成 | `molten_pool.gd`:1.5s 红光预警 → 一次 40 伤 AOE + 屏震 |
| **闪避被动**(翻滚后 2s 受伤 -20%) | ✅ 完成 | `player.gd`:`_evade_buffer_timer` 衰减,`take_damage` 应用减免 |
| **利箭穿透** | ↩️ 已回滚 | 按 csv 默认不穿透,穿透是 Lv4 符文(留待符文系统) |
| **接系统②的 monster_id/level/drop_source 元数据** | ✅ 完成 | spawn_trigger 加 `@export_enum`,运行时挂在 enemy meta;ProgressionManager / LootManager 据此结算 |
| **接系统②的精英组保底通知** | ✅ 完成 | spawn_trigger.elite_group=true → `wave_cleared` → `LootManager.notify_elite_group_killed()` |
| **冰冻 → 解冻后附加 50% 减速 2s** | ✅ 完成 | `enemy_base.apply_slow()` + `_unfreeze` 链式调用;策划 §4.2 寒霜碎裂前置 |
| **女武神改 AOE 伤害技能(原召唤改 instant blast)** | ↩️ 已回滚 | 按 `数值表/skills_active.csv` 锁定值,女武神保持召唤型(20s 协战,CD 45s)。`_execute_melee` AOE 实现保留备用,但 valkyrie 走 SUMMON 路径 |
| **美术资产接入:Player + 屠夫**(部分) | ✅ 完成 | Synty `Characters.fbx` rig 包装,`character_visibility.gd` 过滤显示;Player=Hero_Knight_Female,屠夫=Rock_Golem×1.3。**注:仅静态 T-pose,等美术 D 单独导动画包再接 AnimationTree** |
| Day2 精准被动 / juice 二遍 / 5 件传奇特效 hook | 🟡 等系统组/美术 | 等装备聚合 + 美术资产 |

---

## 二、新增文件结构

```
shaders/
└── hit_flash.gdshader              # ✅ 闪白 + 冰冻染色 二合一 shader

scripts/
├── autoload/
│   ├── focus_resource.gd           # ✅ 专注资源池(100 上限/8 秒回/利箭命中回 4)
│   ├── damage_number_pool.gd       # ✅ 20 个 Label3D 飘字对象池(白普通/黄暴击)
│   ├── combat_juice_manager.gd     # ✅ 监听 hit_landed/enemy_killed/player_damaged 分发反馈
│   └── spawn_manager.gd            # ✅ 全局刷怪 + 波次追踪 (cluster/line/surround)
├── components/
│   ├── screen_shake.gd             # ✅ 挂相机子节点,routing 到 topdown_camera 的 _shake_offset
│   ├── knockback_component.gd     # ✅ 挂敌人子节点,线性衰减击退
│   ├── stagger_component.gd       # ✅ L1 抖动 / L2 僵直,2s 窗口 L2 上限 2 次
│   ├── spawn_trigger.gd            # ✅ Area3D 触发器,玩家进入调 SpawnManager.spawn_wave
│   └── character_visibility.gd     # ✅ Characters.fbx rig 过滤:只显示一个角色 mesh + 选定挂件
├── skills/
│   ├── skill_data.gd               # ✅ Resource:技能定义(投射/位移/召唤通用)
│   ├── skill_slot_manager.gd       # ✅ 5 槽输入/CD/Focus 检查/广播 skill_activated
│   ├── skill_executor.gd           # ✅ 监听 skill_activated 按 skill_type 分发
│   ├── damage_calculator.gd        # ✅ 公式: weapon × mult × (1+dex/100) × crit × (1+ele%)
│   └── data/
│       ├── piercing_arrow.tres     # ✅ 利箭(LMB,0蓝/无CD/155%/命中回4)
│       ├── multishot.tres          # ✅ 多重射击(RMB,20蓝/无CD/110%/5箭60°扇)
│       ├── frost_arrow.tres        # ✅ 冰冻箭(键1,25蓝/无CD/165%/AOE 3m/冻 1.5s + 解冻后 50% 减速 2s)
│       ├── dodge_roll.tres         # ✅ 翻滚(键2,0蓝/CD 6s/4m·0.4s/无敌+取消攻击CD)
│       └── valkyrie_summon.tres    # ✅ 女武神召唤(键3,0蓝/CD 45s/20s 协战/上限 1)
└── entities/
    ├── enemy_base.gd               # ✅ 5 态 AI 基类 (IDLE/CHASE/ATTACK/STAGGER/DEATH) + `is_molten` 词缀钩子
    ├── enemy_data.gd               # ✅ Resource:敌人数值+AI 参数定义
    ├── enemy_zombie.gd             # 重构:extends enemy_base,加载默认 walking_corpse 数据
    ├── valkyrie.gd                 # ✅ 女武神 AI:找最近敌人近战 1.5s/30 伤
    ├── templar.gd                  # ✅ 圣堂武士:FOLLOW/ATTACK 双态 + 10s 治疗 10%
    ├── butcher.gd                  # ✅ 屠夫 Boss:7 态机(含 SWEEP 横扫)
    ├── floor_burn_zone.gd          # ✅ 烧地板单象限:WARNING 2s → BURNING 6s(20%/s)→ 自毁
    ├── molten_pool.gd              # ✅ 熔火精英词缀地面延爆(1.5s→40 伤+屏震)
    └── data/
        └── walking_corpse.tres     # ✅ 缓行走尸数值(80HP/12伤/4.2移速/0.8s前摇)

scripts/ui/
└── hud.gd                          # ✅ 简版 HUD(血/蓝/CD/XP/死亡红屏 + R 重开),后期美术重做

scenes/
├── companions/
│   ├── valkyrie.tscn               # ✅ 金色 capsule + 头顶光晕
│   └── templar.tscn                # ✅ 银白随从,collision_layer=16(ally)
├── enemies/
│   ├── enemy_zombie.tscn           # 重构:加 NavigationAgent3D + Knockback/Stagger 组件
│   ├── butcher.tscn                # ✅ 大红胶囊 + 银屠刀 + 红冲锋预警条 + 圆环普攻预警
│   ├── floor_burn_zone.tscn        # ✅ 4m 圆柱 Area3D,半透红光
│   └── molten_pool.tscn            # ✅ 3m 圆柱 Area3D,频闪橙红警示
├── ui/
│   └── hud.tscn                    # ✅ 空 CanvasLayer,挂 hud.gd
└── arena/test_arena.tscn           # ✅ Wave1 trash + Wave2 elite_blue 熔火 + Boss(全配 monster_id/drop_source/elite_group)
```

---

## 三、对外接口契约(队友重点看这节)

### 3.1 已注册 autoload(战斗组,在 project.godot)
```
FocusResource         # 专注池(系统组掉落/buff 可改 max_focus/regen)
DamageNumberPool      # 20 个 Label3D 飘字池(任意系统调 show_damage)
CombatJuiceManager    # Juice 总分发(监听 CombatManager 信号)
SpawnManager          # 刷怪服务 + 波次追踪
```
配合系统组的 `DataTables / ProgressionManager / Inventory`,9 个 autoload 全在 `[autoload]` 段。

### 3.2 信号契约(供其他域订阅)
```gdscript
# CombatManager (原有,本组负责 emit)
signal hit_landed(attacker, target, damage: int, is_crit: bool, element: String, hit_position: Vector3, hit_direction: Vector3)
signal enemy_killed(enemy, killer, overkill_damage: int, kill_direction: Vector3)
signal player_damaged(amount: int, source)

# SpawnManager
signal wave_started(wave_id: int, count: int)
signal wave_cleared(wave_id: int)         # 全灭时触发,关卡用作"开门"判定
signal enemy_spawned(enemy: Node, wave_id: int)

# FocusResource
signal focus_changed(current: float, max_focus: float)   # UI 蓝条订阅

# SkillSlotManager (Player 子节点)
signal skill_activated(slot_index: int, skill_data: Resource)
signal cooldown_changed(slot_index: int, remaining: float, total: float)  # UI CD 蒙版订阅

# Player
signal health_changed(current: int, max_hp: int)         # UI 血球订阅
signal player_died()
signal dodge_started(direction: Vector3, duration: float)
signal dodge_ended()

# enemy_base
signal died(enemy)
signal state_changed(old_state: int, new_state: int)

# butcher (Boss 专属)
signal phase_changed(new_phase: int)   # 半血触发,关卡 C 可挂演出
signal died(self_ref)                  # 复用 enemy 死亡通知
```

### 3.3 公共 API(队友可调)

```gdscript
# SpawnManager —— 关卡 C 摆放刷怪点用
SpawnManager.spawn_wave({
    "enemy_scene": preload("res://scenes/enemies/enemy_zombie.tscn"),
    "enemy_data":  preload("res://scripts/entities/data/walking_corpse.tres"),  # 可选
    "count": 5,
    "formation": "cluster" | "line" | "surround",
    "center": Vector3,
    "radius": 3.0,
    "target": player_node,    # surround 必填
}) -> int  # wave_id

SpawnManager.clear_wave(wave_id)           # 玩家死亡复位时调
SpawnManager.get_wave_remaining(wave_id) -> int

# FocusResource —— 任意系统可读/写
FocusResource.current / FocusResource.max_focus
FocusResource.consume(amount) -> bool    # 不够返回 false 不扣
FocusResource.gain(amount)
FocusResource.can_consume(amount) -> bool

# DamageNumberPool —— 任意系统/技能调显示飘字
DamageNumberPool.show_damage(world_pos: Vector3, damage: int, is_crit: bool)

# CombatJuiceManager —— 仅 set_freeze 暴露给 enemy_base 用
CombatJuiceManager.set_freeze(target, frozen: bool)   # shader 蓝染

# SkillSlotManager (Player 子节点) —— 系统组 level_up 解锁可调
slot_mgr.set_skill(slot: int, sd: Resource)
slot_mgr.cancel_cooldown(slot: int)
slot_mgr.get_cooldown_remaining(slot: int) -> float

# Player —— 受外部状态影响时调
player.dodge(direction: Vector3, distance: float, duration: float) -> bool
player.take_damage(amount: int, source)
player.get_movement_input_direction() -> Vector3

# enemy_base —— 任意伤害源调
enemy.take_damage(amount: int, source)
enemy.apply_status(effect: String, duration: float)   # "frost" 触发冻结
```

### 3.4 SpawnTrigger 节点(关卡 C 摆刷怪点)

```gdscript
@export var enemy_scene: PackedScene
@export var enemy_data: Resource
@export var count: int
@export_enum("cluster","line","surround") var formation: String
@export var spawn_radius: float
# ── 系统组对接元数据(决定 XP / 掉落分级)──
@export_enum("trash","elite_blue","champion_yellow","skeleton_guard","butcher") var monster_id: String
@export_range(0,8) var monster_level: int  # 0=运行时取玩家等级
@export_enum("trash","elite_blue","champion_yellow","butcher") var drop_source: String  # → DropSystem.Source 枚举
@export var elite_group: bool  # true → wave_cleared 时调 LootManager.notify_elite_group_killed()
@export var spawn_as_molten: bool  # true → 本波怪打"熔火"词缀(死亡 1s 后地面延爆)
```

> 自动行为:本波每只怪在 SpawnManager.enemy_spawned 信号触发时被打上 monster_id / monster_level / drop_source 三条 meta;ProgressionManager 据此查 monsters.tres 的 base_xp,LootManager 据 drop_source 选权重表。

---

## 四、关键设计决策(已落地,队友需了解)

### 4.1 数值口径(策划案 V2.0 对齐)
- **专注资源**:上限 100 / 被动 8/秒 / 利箭命中回 4 / 多重 25 蓝 / 冰冻箭 25 蓝 ✓
- **5 主动技能**全部就位:利箭(LMB) / 多重(RMB) / 冰冻箭(键1) / 翻滚(键2) / 女武神(键3)
- **缓行走尸**:80 HP / 12 攻击 / 4.2 移速 / 0.8s 前摇 / 2m 攻击范围(策划 03 文档锚点)
- **伤害公式**:`weapon_avg × skill_mult × (1+敏捷/100) × 暴击 × (1+元素%)` 五乘区(占位 weapon=15, dex=20, crit=5%/×1.5, ele=0%)

### 4.2 工程决策

1. **避开 Godot 4 跨文件 class_name 解析竞态**:战斗组所有自定义 Resource(`SkillData` / `EnemyData`)和管理类(`SkillSlotManager` / `SkillExecutor` / `SpawnManager`)**不用 class_name**,改用 `const X = preload(...)` + 弱类型 `Resource` 引用。这是踩过的坑 —— autoload 列表大改后首次扫描会触发 `Compilation failed (error code: 43) at line 1`,跟实际代码无关。**已知症状**:本次 pull 系统组提交后,他们的 `data_tables.gd` / `affix_table.gd` 等 8 个引用 class_name 的下游文件触发同症状。建议系统组在编辑器 reload 后观察,或参考战斗组同样改成 preload。

2. **Juice 通过 shader 实现**:`hit_flash.gdshader` 提供 `flash_intensity` + `freeze_intensity` 两个 uniform,CombatJuiceManager 在第一次命中时 lazy 创建 ShaderMaterial 缓存。enemy 不需要预先准备 shader,只要有 BodyMesh 子节点即可。

3. **状态机**:enemy_base 用 enum + match,**不用** AnimationTree/StateMachine 节点(后者对编辑器依赖重)。后续接动画时只需在 `_set_state` 里加 `animation_player.play()`。

4. **Stagger / Knockback 解耦为组件**:挂在 enemy 子节点,通过 NodePath 引用 mesh。这样 7 种白怪共享同一套组件,不用每种怪重写。

5. **AI 优先级序**:`DEATH > knockback.is_active() > is_frozen > stagger > AI state` —— 在 `_physics_process` 顶部短路 return,保证组件接管时 AI 完全让位。

### 4.3 已知问题 & 已修
- ❌ 早期 ATTACK windup 期间敌人完全停止 → 玩家 7m/s 0.8s 内能跑 5.6m → 永远空挥
  ✅ 修:windup 期间允许 50% 移速漂移维持身位 + strike 容差 1.25× → 1.5×
- ❌ NavigationAgent3D 的 RVO 避让在多怪密集时死锁
  ✅ 修:enemy_zombie.tscn 关掉 `avoidance_enabled`,加 NavAgent finished 兜底直接朝玩家
- ❌ 玩家被打没有反馈,分不清"没被打"还是"被打了但看不见"
  ✅ 修:CombatJuiceManager 监听 `player_damaged` → mesh 闪白 + 屏震 0.08m × 0.15s + 头顶飘字

---

## 五、待办与对接事项

### 待我做(Day2 剩余)
- [ ] **精英词缀行为**:熔火(地面延爆),冰冻是 stretch
- [ ] **2 被动**:闪避(X% 免一次伤)+ 精准(暴击率/暴伤),需要系统②的属性聚合接口
- [ ] **强力射击(穿透)**:技能 6 是 stretch,先用现有 piercing_arrow 的 `can_penetrate=true` 扩展
- [ ] juice 第二遍:Boss 命中音效/演出,这部分等角色 D 出资产
- [ ] 屠夫 v2(可选):肉钩、践踏、P2 全场轮燃、新场景、检查点

### P1 缺口(已排查,待决策/排期 — 2026-06-13)
- [ ] **梦魇怪物缩放未实现**:`数值表/nightmare.csv` 定义了系数(梦魇1 生命×1.5/伤害×1.25,梦魇2 ×2.2/×1.5),但战斗端 enemy 数值未应用。系统侧仅 `LootManager.set_nightmare_tier()` 管掉落橙权重,无怪物属性缩放。缺一个全局"当前梦魇层"状态源 + enemy 数值初始化时读系数。demo 测试场未进梦魇层,非阻塞。归属待定(怪物数值=战斗域,但 tier 状态可能放系统域)。
- [ ] **Focus 词缀缺口**:`affixes.csv` 10 条词缀无 Focus 相关项;`FocusResource` 已暴露 `max_focus/regen` 可写,但无装备词缀投递。若策划要"+最大专注"传奇,需先在 affix 表加条目(策划)。当前非阻塞。
- [ ] **HUD 属性显示口径**:`hud.gd` 占位,逻辑层可读 level/xp/敏捷/体能/maxHP;伤害显示口径(武器伤/技能伤/最终伤)待美术 D 重做时定。

### 需队友同步

1. **系统② 已对接(close)**
   - ✅ `level_up.unlocked` id 与 `xp_curve.tres` unlocks 表对齐:`skill_puncture/skill_multishot/skill_frost_arrow/skill_roll/skill_valkyrie` → 5 主动技能;`slot_N`/`passive_*`/`rune_*`/`tier_*` 暂跳过(被动池 / 符文 / Tier 武器后续接)。
   - ✅ 怪物 meta 全套挂上:`monster_id`(StringName)+ `monster_level`(int)+ `drop_source`(int=DropSystem.Source);通过 SpawnTrigger 自动设。屠夫场景里也直接 set_meta 作为 fallback。
   - ✅ 精英组保底通知:SpawnTrigger.elite_group=true → `wave_cleared` 自动调 `LootManager.notify_elite_group_killed()`。
   - ✅ **`Inventory.stats_changed(total_stats)` 已联通(2026-06-13 close "换装变强" V2)**:`SkillExecutor._ready()` 连接 `/root/Inventory.stats_changed` → `DamageCalculator.refresh_from_stats(total_stats)` 整体重算面板。口径(对齐 数值表/affixes.csv + player_combat_reference.csv):
     - `weapon_avg` = `WEAPON_DAMAGE` 词缀值,**完全由装备决定**,未装弓回退基底 15。
     - `dexterity` = `AGILITY`(get_total_stats 已含升级基础敏捷 + 词缀)。
     - `crit_rate` = 基底 5% + `CRIT_CHANCE` 词缀(词缀单位是%,/100)+ 精准被动。
     - `crit_damage` = 基底 ×1.5 + `CRIT_DAMAGE` 词缀(%,/100)+ 精准被动。
     - `elemental_bonus` 暂无对应词缀,占位 0.0。
   - ✅ **精准被动重构**:`apply_precision_passive()` 改用独立被动字段(`_precision_crit_bonus/_precision_cd_bonus`),`refresh_from_stats` 重算时叠加 → 换装重算不再覆盖被动加成。
   - ✅ **随从伤害接装备**:女武神 = `DamageCalculator.weapon_avg × 2`、圣堂武士 = `× 1.5`,`_do_strike` 实时取值(`_current_damage()`),装备/升级即时反映;weapon_avg 未就绪时回退占位(30/22)。

2. **关卡 C 已就绪**
   - SpawnTrigger 配置已扩到含元数据(见 §3.4);摆遭遇直接调 `SpawnManager.spawn_wave({...})` 或丢 SpawnTrigger 节点,3 种 formation 可用。
   - `wave_cleared(wave_id)` 作"开门"判定。
   - 测试场 `test_arena.tscn` 含 3 个 SpawnTrigger 演示:Wave1 trash surround 5 / Wave2 elite_blue 熔火 cluster 8(elite_group 喂保底) / Boss 屠夫(monster_id=butcher, drop_source=butcher, level=7)。
   - 后续梦魇关卡进入时调 `LootManager.set_nightmare_tier(tier)` 叠橙权重(系统组已暴露)。

3. **角色 D 已铺底,等资产**
   - `CombatManager.hit_landed` / `enemy_killed` 是 juice 入口,挂粒子/音效。
   - `dodge_started(direction, duration)` / `dodge_ended` → 翻滚动画。
   - `stagger_started(level, duration)` → 受击动画(StaggerComponent 暴露)。
   - Player 的 `health_changed` / `player_died` → 受击/死亡演出。
   - 5 个 `.tres` 技能数据已就位,接 AnimationPlayer 时按 `skill_id` 关联状态。
   - 屠夫的 `phase_changed(2)` → 半血咆哮演出钩子。

---

## 六、测试场说明

`scenes/arena/test_arena.tscn`:
- 20×20 平面 + NavigationRegion3D(单 quad 烘焙过的 navmesh)
- 3 个 SpawnTrigger:
  - **Wave1** 在 (0, 0.5, -4),触发圈半径 3m,玩家出生点北偏后即触发 → 5 僵尸在玩家周围 6m 圆周 surround
  - **Wave2** 在 (7, 0.5, 0),往右走 7m 触发 → 8 僵尸在 (7,0,0) 4m 内 cluster
  - **Boss** 在 (12, 0.5, 0),继续向右走触发 → 屠夫 spawn(`butcher.tscn`,one_shot)
- 玩家出生点 (0, 0, 0),朝向 -Z;Templar 在 (1.5, 0, 0)

完整闭环验收:
1. 走出生点北 → 触发 Wave1 → 5 僵尸 surround
2. 僵尸追上来打 → 玩家 mesh 闪白 + 屏震 + 头顶 12 飘字
3. 玩家左/右键射击 → 闪白 + 飘字 + 击退 + L1/L2 抖
4. 按 1 用冰冻箭 → 命中 3m 蓝光 + 范围内变蓝定身
5. 冻住的怪被打死 → 7 个蓝色碎块飞散
6. 按 2 翻滚 4m + 0.4s 无敌
7. 按 3 召唤金色女武神,自动追怪 1.5s/30 伤
8. 5 只全杀光 → emit `wave_cleared(wave_id)`
9. 继续右走触发 Wave2 → 8 僵尸 cluster
10. 再向右触发 Boss → 屠夫慢悠悠走来普攻 → 拉远引发冲锋(亮红色预警)→ 引诱撞墙 → **3s 硬直窗口爆输出(承伤 +25%)**
11. 半血触发咆哮 1.5s 无敌 → P2 攻速 +30%、烧地板间隔 18→12s
12. 烧地板:玩家附近 3-6m 随机位置,2s 红光预警 → 6s 燃烧(站内 20%/s)
13. 击杀屠夫:屏震 + 缩放消失,系统组按 `monster_id=butcher / monster_level=7` 给 XP

## 七、屠夫 Boss MVP 说明(策划 03 §6)

**数值锚点**(`butcher.gd`):
- HP 11000,普攻 75 伤,移速 4.0,冲锋 14.0
- 普攻三阶段:windup 0.7s + strike 0.25s + recovery 0.5s,3.0m 触发,3.5m 命中容差
- 冲锋:windup 1.2s + dash(最长 25m) + 撞墙硬直 3s(承伤 ×1.25),触发条件玩家 > 6m
- P2 触发线:HP ≤ 50% → ROAR 1.5s(无敌)→ 攻速 +30%、烧地板间隔 18→12s
- 烧地板:首次 40s 后,玩家附近 3-6m 随机方位,WARNING 2s → BURNING 6s(0.5s tick × 10% maxHP = 20%/s)→ 自毁

**控制免疫(策划 03 §7.2)**:
- 不挂 KnockbackComponent → 击退完全无效
- StaggerComponent 只接视觉,`stagger_started` 信号 **故意不连接** 到 butcher.gd → 不打断 AI
- `apply_status("frost")` 改为 30% 减速 + 短 L2 视觉,免冻

**留 v2**:肉钩、践踏、P2 全场轮燃、新场景、检查点

