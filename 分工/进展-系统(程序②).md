# 系统(程序②) · 工作进展沉淀

> 维护者:程序② / 引擎 Godot 4.6.3 / GDScript
> 最近更新:2026-06-14（🆕 功能塔策划定案：伤害塔/加速塔互动激活全局 buff、互斥二选一，已写入重切方案 §2.5，详见 §7.11；前序修复 boss 房占位走尸→真屠夫，打通掉落+结算全链路，详见 §7.10）

本文档持续记录系统域的实现进展、对外接口契约、待办与队友对接事项。**队友(战斗①/角色D/关卡C)对接接口前先看本文档。**

---

## 一、当前进度总览

| 任务 | 状态 | 说明 |
|------|------|------|
| Hour 0 工程地基 | ✅ 完成 | 目录骨架 / autoload 结构 / 数据表 schema |
| 任务1 数据表加载器 | ✅ 完成 | `DataTables` autoload + 查询 API,**战斗①可对接** |
| 对外信号契约 | ✅ 完成 | 3 个新 autoload 信号定义,**战斗①/角色D可对接** |
| 数据表 .tres | ✅ 完成 | 已对齐 `数值表/*.csv` **V2.1**(屠夫锁定值/新增骸骨卫士),headless 锁定值断言通过 |
| 任务2 物品/词缀系统 | ✅ 完成 | `ItemGenerator` 按品质+部位 roll,实测通过 |
| 任务3 掉落与拾取 | ✅ 完成 | 击杀→掉落判定→地面名牌→F 拾取入包,headless 实测+连场景可玩 |
| 任务4 背包+装备 | ✅ 完成 | 40格背包 / 13装备槽 / 装备生效改属性 + B 键开关 UI,headless 实测通过(详见 §3.7) |
| 任务5 XP/升级 | ✅ 完成 | 击杀→XP→跨级升级→属性成长→发 level_up,实测通过 |
| 战斗①对接验证 | ✅ 完成 | 战斗组已挂 monster_id/level/drop_source meta + 精英组保底通知,端到端 headless 验证全通过(详见 §6.3) |

---

## 二、新增文件结构

```
scripts/
├── autoload/
│   ├── data_tables.gd          # ✅ 任务1:数据表加载器(战斗①在等)
│   ├── progression_manager.gd  # ✅ 任务5:XP/等级/属性成长(发 level_up)
│   ├── loot_manager.gd         # ✅ 任务3:击杀→掉落→地面 spawn 全链路接线
│   └── inventory.gd            # ✅ 任务3 入包 + 任务4 装备/卸下/属性聚合(发 stats_changed)
├── systems/
│   ├── item_generator.gd       # ✅ 任务2:物品/词缀生成器
│   └── drop_system.gd          # ✅ 任务3:掉落判定纯逻辑(源×品质权重/保底)
├── entities/
│   └── loot_drop.gd            # ✅ 任务3:地面掉落物(名牌+范围+F拾取)
├── data/                       # 强类型 Resource schema
│   ├── affix_def.gd            # 词缀定义(类别/数值范围/可出现部位)
│   ├── affix_table.gd          # 词缀池容器
│   ├── legendary_def.gd        # 传奇定义(部位/特效id/首橙白名单)
│   ├── legendary_table.gd      # 传奇容器
│   ├── monster_def.gd          # 怪物定义(等级缩放公式)
│   ├── monster_table.gd        # 怪物容器
│   ├── xp_curve.gd             # 经验曲线/属性成长/每级解锁
│   ├── tier_table.gd           # 武器/防具 Tier 台阶(T1~T3)
│   ├── equip_slots.gd          # 13 装备槽枚举+名称映射
│   ├── item_instance.gd        # 运行时物品实例(品质/词缀/聚合属性)
│   ├── item_base_def.gd        # ✅ 基底定义(部位/T1~T3名/主属性类型/可roll词缀)
│   ├── base_item_table.gd      # ✅ 基底池容器(12部位×3Tier)
│   ├── epic_item_def.gd        # ✅ 史诗具名定义(部位/保送词缀/build倾向)
│   └── epic_item_table.gd      # ✅ 史诗具名池容器(10件)
scenes/
└── loot/
    └── loot_drop.tscn          # ✅ 任务3:地面掉落物场景(宝石+光柱+名牌)
scenes/ui/
└── inventory_panel.tscn        # ✅ 任务4:背包+装备 UI(CanvasLayer, B 键开关)
scripts/ui/
└── inventory_panel.gd          # ✅ 任务4:程序化背包网格+装备槽+属性面板
data/                           # 实际数据资源(.tres)
├── affixes.tres                # 10 条词缀
├── legendaries.tres            # 5 件传奇
├── monsters.tres               # 普通/蓝精英/黄首领/屠夫
├── xp_curve.tres               # 1~8 级曲线
├── tier_table.tres             # 3 个 Tier
├── base_items.tres             # ✅ 12部位×3Tier 基底名表(程序化命名用)
└── epic_items.tres             # ✅ 10 件史诗具名表(保送词缀)
tools/                          # 工具脚本(非运行时)
├── gen_data_tables.gd          # 重新生成所有 .tres(换表时重跑)
├── verify_data_tables.gd       # headless 验证加载器
├── verify_item_gen.gd          # headless 验证物品生成器
├── verify_progression.gd       # headless 验证 XP/升级系统
├── verify_drop_system.gd       # headless 验证掉落系统+入包
├── verify_inventory_equip.gd   # headless 验证背包+装备+属性聚合(任务4)
├── verify_knockback_death.gd   # headless 回归:怪物死亡时击退自动取消(det==0 修复)
└── verify_inventory_key.gd     # headless 回归:背包开关键=B(真实加载 main.tscn 走输入管线注入 B 键验证面板翻转)
```

---

## 三、对外接口契约(队友重点看这节)

### 3.1 DataTables 查询 API(战斗①对接)
```gdscript
DataTables.get_all_affixes() -> Array[AffixDef]
DataTables.get_affix(id) -> AffixDef
DataTables.get_affixes_for_slot(slot_name) -> Array[AffixDef]   # roll 词缀用
DataTables.get_legendary(id) -> LegendaryDef
DataTables.get_first_orange_whitelist() -> Array[LegendaryDef]  # 首橙保底
DataTables.get_monster_stats(id, level) -> { health, attack, xp }
#   怪物 id: trash / elite_blue / champion_yellow / skeleton_guard(骸骨卫士) / butcher(屠夫)
DataTables.get_xp_to_next(level) -> int     # -1 = 已满级
DataTables.get_tier_for_level(level) -> int
DataTables.xp_curve / DataTables.tier_table  # 直接访问曲线对象
```

### 3.2 信号契约
```gdscript
# ProgressionManager —— 战斗① 接 level_up 解锁技能
signal xp_gained(current_xp: int, xp_to_next: int, level: int)
signal level_up(new_level: int, unlocked: Array)
signal stats_changed(stats: Dictionary)   # 升级后主属性变化(HUD/战斗①刷新)
#   unlocked 示例: ["slot_2", "skill_multishot"]  (来自 xp_curve.unlocks)

# Inventory —— 角色D 接 loot_dropped 挂光柱/音效
signal loot_dropped(quality: int, world_pos: Vector3)   # quality=ItemInstance.Quality
signal item_picked_up(item: ItemInstance)
signal item_equipped(slot: int, item: ItemInstance)     # slot=EquipSlots.Slot
signal item_unequipped(slot: int, item: ItemInstance)
signal stats_changed(total_stats: Dictionary)           # HUD/面板刷新
```

### 3.3 已注册 autoload(project.godot)
```
GameManager / CombatManager (原有)
DataTables / ProgressionManager / Inventory / LootManager (新增)
```

### 3.4 ItemGenerator API(任务2,掉落系统/调试用)
```gdscript
var gen := ItemGenerator.new(DataTables)           # 传入 DataTables autoload, 可选第2参 seed
gen.generate(slot, item_level)                     # 品质按 5 档权重(普通55/精良33/稀有10/史诗1.5/传说0.5)随机,源 drop_table.csv
gen.generate(slot, item_level, ItemInstance.Quality.RARE)  # 指定品质
gen.generate_legendary(&"windforce_boots", 8)      # 指定传奇
gen.roll_quality() -> int                          # 单独抽品质
```
- 5 档词缀数:普通 0~1 / 精良(蓝)1~2 / 稀有(黄)3~4 / 史诗(紫)4~5+保送1顶值 / 传奇(橙)4+橙字特效(不占位);按部位过滤词缀;按等级定 Tier roll 数值。**(完整 5 档枚举/权重事实源见 §6.5;命名规则见 §6.6)**
- 返回 `ItemInstance`,其 `aggregate_stats()` 给 Inventory 算面板。

### 3.5 ProgressionManager API(任务5,战斗①/HUD 对接)
```gdscript
ProgressionManager.add_xp(amount)        # 加 XP,跨级自动循环升级
ProgressionManager.get_stats() -> { level, current_xp, xp_to_next, agility, vitality, max_hp }
ProgressionManager.is_max_level() -> bool
ProgressionManager.level / current_xp / agility / vitality / max_hp   # 直接读字段
```
- **击杀自动给 XP**:`_ready` 里连 `CombatManager.enemy_killed`。怪物实例若挂 `monster_id`/`monster_level` 元数据则据此查表;**未挂元数据时兜底按 `trash`@玩家当前等级**,战斗①接入前即可跑通。
- 跨级:单次大 XP 连升多级,逐级发 `level_up`。满级(8)后 XP 不再溢出/不越级,经验条置满。
- 升级即按 `xp_curve` 刷新敏捷/体能/生命并发 `stats_changed`。
- headless 实测:`tools/verify_progression.gd`(L1→800XP→L3 余20、属性成长、解锁 payload、满级封顶、缺省兜底,全部 `VERIFY OK`)。

### 3.6 掉落系统 DropSystem / LootManager(任务3,战斗①/关卡C 对接)
```gdscript
# DropSystem —— 纯逻辑掉落判定(RefCounted, 可 headless 测), 一般由 LootManager 调度
var ds := DropSystem.new(DataTables, item_generator, seed)
ds.roll_drop(DropSystem.Source.TRASH, player_level) -> Array[ItemInstance]  # 空=不掉
ds.register_elite_kill()        # 杀完一组精英喂软保底(每组 +2% 橙)
ds.tick(delta)                  # 每帧推进保底计时(硬保底 8 分钟首橙)
ds.set 字段: nightmare_tier(0/1/2 梦魇层叠橙权重)
ds.get_legendary_count()        # 本局已掉传奇数(结算 n/5 钩子)

# LootManager(autoload)—— 击杀→掉落→地面 spawn 全链路, 战斗①只需挂元数据
LootManager.notify_elite_group_killed()   # 战斗① 杀完一组精英时调一次
LootManager.set_nightmare_tier(tier)      # 关卡C 进梦魇层时设置
```
- **掉落源**:`TRASH / ELITE_BLUE / CHAMPION_YELLOW / BUTCHER / CHEST_COMMON / CHEST_FANCY`,掉落率+件数+蓝黄橙权重严格对齐策划 §6.1。
- **战斗① 对接**:怪物实例挂 `drop_source`(int=DropSystem.Source)+ `monster_level`(int)元数据,LootManager 接 `CombatManager.enemy_killed` 自动在怪物位置 spawn 地面掉落物。**未挂时兜底 TRASH@玩家等级,不阻塞联调。**
- **保底**:首件传奇必∈白名单(女妖弓/冰霜箭袋/疾风靴);前 4 件传奇查重不重复;硬保底 8 分钟无橙则下个保底载体强制首橙。
- **拾取**:`loot_drop.tscn` 走近(范围 Area3D)+ 按 `interact`(F 键)拾取 → `Inventory.add_item` → 满包(40)留在地上(DUI-05 手动拾取口径)。
- headless 实测:`tools/verify_drop_system.gd`(TRASH 18.4% / 精英 100% 且 59-38-3 / 首橙白名单 200局命中 / 硬保底首橙 / 前4件查重 / 入包满包封顶,全部 `VERIFY OK`)。

### 3.7 Inventory 背包+装备 API(任务4,角色D/HUD/战斗①对接)
```gdscript
# 背包(40 格)
Inventory.add_item(item) -> bool          # 满包(40)返回 false,物品留地面
Inventory.bag_count() -> int
Inventory.is_full() -> bool
Inventory.get_bag_items() -> Array[ItemInstance]   # 只读快照(UI 渲染用)

# 装备(13 槽, slot = EquipSlots.Slot 0~12)
Inventory.equip(slot, item) -> bool       # item 须在背包;旧装备回背包(满包则失败保持原状)
Inventory.unequip(slot) -> ItemInstance   # 回背包(满包返回 null);空槽/非法槽返回 null
Inventory.get_equipped(slot) -> ItemInstance       # 空返回 null
Inventory.quick_equip(item) -> int        # 按 item.slot 自动选槽;戒指自动路由 RING_1→RING_2;返回落位 slot,失败 -1

# 属性聚合(基础主属性 + 已装备词缀)
Inventory.get_total_stats() -> Dictionary # { AffixDef.StatKind(int): value(float) }
```
- **属性生效**:`get_total_stats` 取 `ProgressionManager.agility/vitality` 作基础,叠加所有已装备件的 `aggregate_stats()` 词缀加成。任一装备/卸下都发 `stats_changed(total_stats)` → HUD/角色面板/战斗① 刷新。
- **换装语义**:换装时旧装备回填到新装备腾出的格子,**净占用不变**;直接装备地面物(不在背包)时旧装备需新增一格,满包则装备失败。
- **戒指双槽**:RING_1/RING_2 独立;`quick_equip` 自动选空槽,两戒满则替换 RING_1。
- **UI**:`scenes/ui/inventory_panel.tscn`(程序化 CanvasLayer,layer=110 盖 HUD)——`B` 键(`toggle_inventory` 动作)开关,左 40 格网格(点击=quick_equip)、右 13 槽(点击=卸下)、下属性面板(监听 `stats_changed` 实时刷新)。
- headless 实测:`tools/verify_inventory_equip.gd`(40 格溢出 / 装备-卸下信号 / 基础+词缀聚合 / 换装净占用不变 / 戒指双槽 quick_equip,全部 `VERIFY OK`)。

---

## 四、关键数值锚点(已落进 .tres,对齐 `数值表/*.csv` V2.1,实测通过)

- **词缀池**:10 条(6 进攻/3 生存/1 功能),T1/T3 范围,T2 线性内插 — 与 `affixes.csv` 一致
- **品质(5 档,扩展层 opt-in)**:普通(白/灰,0~1 词缀) / 魔法(蓝,1~2) / 稀有(黄,3~4) / 史诗(紫,4~5,**保送1条顶值词缀**) / 传奇(橙,4+特效不占位)。枚举值劣→优单调:`COMMON=0/MAGIC=1/RARE=2/EPIC=3/LEGENDARY=4`。权重事实源 = `drop_table.csv`(见 §6.5)
- **传奇 5 件**:女妖之弓 / 冰霜之箭袋 / 疾风之靴(首橙白名单) / 女武神之冠 / 专注引擎 — 与 `legendaries.csv` 一致
- **怪物公式**:白怪生命 `40×1.31^(L-1)`、攻击 `15×1.17^(L-1)`、经验 `8×1.45^(L-1)`(L8≈265/45/107)
- **精英倍率**:蓝名 ×2.5 生命/×1.3 攻;黄名 ×5 生命/×1.4 攻 — 与 `constants.csv` 一致
- **骸骨卫士盾兵**:白怪 ×1.0 生命/×1.3 攻/×1.5 经验(`skeleton_guard`,base_xp=12)— **扩展层 P0 修正:生命 1.6→1.0**(靠正面120°非穿透-75%破盾而非堆血,见 `数值表/扩展-…差异化.md` §四),`monsters.tres` + `gen_data_tables.gd` 均已落
- **屠夫**(唯一 Boss):`health_mult=55.0`/`attack_mult=1.95`,锁定 @7 生命≈11119≈11000 / 攻≈75(`constants.csv` 白怪×约55/攻×约2;D1 已修正非×5)
- **XP 曲线**:`[300,480,768,1229,1966,3146,5033]`(L1→L8,×1.6 累计≈12922)— 与 `player_growth.csv` 一致
- **属性成长**:敏捷 +6/级(L8=52)、体能 +4/级(L8=38)、生命 `40+10L+10×体能`(L8=500)— 与 `player_growth.csv` 一致
- **装备槽**:13 槽 / 背包 40 格
- **武器 Tier**:T1(Lv1~3) / T2(Lv4~6) / T3(Lv7~8),DPS 3-8 / 9-18 / 20-32 — 与 `player_growth.csv` 一致
- **梦魇橙权重**:层1 ×1.5 / 层2 ×2.0 — 与 `nightmare.csv` 一致

> 单一事实源 = `数值表/*.csv`(V2.1)。换表后:改 `tools/gen_data_tables.gd` 重跑,再 `tools/verify_data_tables.gd` 核对 CSV 锁定值断言。
>
> **导入修复(2026-06-13)**:Godot 默认用 `csv_translation` 导入器把 `数值表/*.csv` 当本地化翻译,中文表头(尤其 `loot_timeline.csv` 含 `/` 的「品质/件」列)生成 `xxx.品质/件.translation` 非法路径反复报错。已将全部 11 个 `.csv.import` 改为 `importer="keep"`(纯数据保留、不导入),删除所有垃圾 `.translation` + 清理 `.godot/imported` 缓存,headless `--import` 已干净通过。**新增 CSV 时记得把其 `.import` 也设为 keep。**

---

## 五、待办与对接事项

### 待我做(任务 4)
- [x] 任务3:掉落与拾取(击杀掉落概率表→地面名牌→F 拾取入包)✅
- [x] 任务4:背包(40格)+装备(13槽)+属性聚合生效 + B 键开关 UI ✅
- [x] 任务5:XP/升级完整逻辑(ProgressionManager 的 add_xp / 跨级 level_up / 属性成长)✅

### 需队友同步
1. ~~**战斗①**:怪物实例挂 `monster_id`+`monster_level` 元数据~~ ✅ **已对接**(`spawn_trigger.gd` `_on_enemy_spawned` set_meta),端到端验证通过。
2. ~~**战斗①**:挂 `drop_source` 元数据 + 杀完精英组调 `LootManager.notify_elite_group_killed()`~~ ✅ **已对接**(`elite_group` 导出 + `wave_cleared` 信号接通),验证通过。
3. **战斗①**:`level_up` 的 `unlocked` 字符串(见 `xp_curve.gd` unlocks 表:slot_x / skill_xxx / passive_xxx / rune_xxx)需与你们技能/被动/符文 id 对齐。
4. **角色D**:`loot_dropped(quality, world_pos)` 用于挂掉落光柱/音效;`stats_changed`(升级)用于刷新 HUD/面板。
5. **关卡C**:`data/*.tres` 已对齐 `数值表/*.csv` **V2.1**;后续换表改 `tools/gen_data_tables.gd` 重跑 + `verify_data_tables.gd` 核对,接口不变;进梦魇层调 `LootManager.set_nightmare_tier(tier)` 叠橙权重。
6. ~~**战斗①**:剩唯一未联通项是 `Inventory.stats_changed`(装备生效)~~ ✅ **任务4 已落地**,装备/卸下即发 `stats_changed(total_stats)`,战斗①可接此刷新装备加成后的战斗属性。
7. **关卡C(命名引用审计 flag,2026-06-13)**——系统②对全仓 9 份相关文档做了一轮"稀有度命名引用一致性"审计,**命名规则本身零冲突**(`掉落物清单-说明.md` §三 与 `rarity_naming.csv` 逐项一致)。以下属策划自有文档(`数值表/*` + `暗黑破坏神3-Demo策划案/*`),系统②不擅改,列此供你处理:
   - **[P0] 3↔5 档映射缺口**:`00-Demo总案`、`04-Demo装备与掉落`、`07-DemoUI` 三份均声明"品质 3 档(魔法/稀有/传奇)",但未注明这是 5 档真值(普通/精良/稀有/史诗/传奇)的**垂直切片**(省略普通=白·史诗=紫)。易被误读为产品级定义,建议各处补一行"Demo 限定 3 档,完整 5 档命名真值见 `数值表/rarity_naming.csv` 与 `掉落物清单-说明.md` §三;蓝=精良/黄=稀有/橙=传奇"。
   - **[P1] 源表交叉引用缺失**:`04`(传奇清单/词缀池/词缀生成/词缀来源 4 处)、`00`(G5+系统④约束)、`07`(首橙白名单)描述了具名传奇/词缀/命名却未指向源表,建议补"传奇见 `legendaries.csv`;词缀命名素材见 `affix_naming.csv`;基底/史诗/命名模板见 `base_items.csv`/`epic_items.csv`/`rarity_naming.csv`"。
   - **[P2] README 索引补全**:`README-数值表.md` 已索引 base_items/affix_naming/epic_items/legendaries,但漏列模板总表 `rarity_naming.csv`,建议补一行"`rarity_naming.csv`:5 档命名模板总表(§三沉淀)"。
   - **[已确认无需动]** `06-Demo终局挑战`、`扩展-稀有度-掉落-敌人差异化.md` 不涉及命名;`掉落物清单-说明.md` 即规则源,无需反向引用 `rarity_naming.csv`。

---

## 六、运行与可见性说明(联调答疑)

**Q:在关卡场景里跑,为什么看不到血量条 / 背包?(2026-06-13)**

- **根因**:HUD(`hud.tscn`)与背包面板(`inventory_panel.tscn`)原先**只挂在 `main.tscn`**;关卡组的可玩场景 `scenes/levels/level_01_play.tscn` / `level_02_play.tscn` 含 Player+Camera 但**未挂这两个 UI 节点**,故 F5 跑关卡时无血量/背包显示。(headless 实测 `main.tscn` 的 HUD 一切正常:血量 200/200、Lv/XP 正常、Player 已绑定,确证非 HUD 自身 bug。)
- **修复**:给两个 `*_play.tscn` 各补挂 `HUD`(id 5)+ `InventoryPanel`(id 6)节点。headless 复验两关卡:HUD 可见、血量条 200/200、InventoryPanel 已挂、Player 已绑定,全部 `VERIFY OK`。
- **背包**仍按 `B` 开关(默认隐藏=设计预期),关卡里按 B 即可弹出。
- **注**:`*_play.tscn` 属关卡组(程序①)场景文件,本次为 UI 联调补挂,未改关卡布局/逻辑。

**Q:任务5 这些工作能在 Godot 里跑起来 / 看到吗?**

- **逻辑层已可运行**:`main.tscn` 已放 5 个僵尸,F5 击杀 → `CombatManager.enemy_killed` → `ProgressionManager._on_enemy_killed` → `add_xp`,XP 在内存里真实累加;headless `tools/verify_progression.gd` 全程 `VERIFY OK`。
- **当前游戏窗口里"肉眼看不到"**,因为缺三块展示/接入(均非系统域阻塞项):
  1. **无 HUD**:`main.tscn` 仅竞技场+玩家+相机,没有显示等级/经验的 UI(待角色D 接 `xp_gained`/`level_up`/`stats_changed`)。
  2. **怪未挂等级元数据**:僵尸无 `monster_id`/`monster_level`,走兜底逻辑——按 `trash`@玩家等级,每杀 **+8 XP**(待战斗① 接入真实怪物 id/等级)。
  3. **场上怪太少**:L1→L2 需 300 XP ≈ 杀 38 只,而竞技场只有 5 只,凑不够一次升级。
- **结论**:系统域逻辑交付完毕、可运行、已验证;"看得见"取决于 HUD(角色D)+ 怪物元数据(战斗①)接入,属展示层/联调阶段工作。

**Q:任务3 掉落能在 Godot 里看到/玩到吗?**

- **可以,连场景可玩**:F5 → 走进竞技场触发刷怪(战斗① 的 `SpawnTrigger` → `SpawnManager` 动态生成)→ 击杀怪物 → `CombatManager.enemy_killed` → `LootManager` 在死亡位置 spawn 地面掉落物 → 走近 → 按 **F** 拾取入包。
- **地面物**:`loot_drop.tscn` 自带宝石 Mesh + 漂浮/旋转动画 + `Label3D` billboard 名牌(蓝/黄/橙按品质上色,弓追加 DPS),传奇额外显示光柱。
- **当前掉率**:`test_arena` 三个触发器已挂 `drop_source`/`monster_level` 元数据(Wave1 trash / Wave2 elite_blue / Boss butcher@7),正式分级掉落生效;未挂元数据的怪仍兜底 **TRASH@玩家等级(18%)**。
- headless 逻辑验证:`tools/verify_drop_system.gd` 6 项判定全部 `VERIFY OK`。

### 6.3 战斗①对接验证(2026-06-13)

战斗组已在 `scripts/components/spawn_trigger.gd` 接入系统组接口:
- `_on_enemy_spawned` 给本波每只怪 `set_meta` 三条:`monster_id`(值为 StringName)/`monster_level`(int,屠夫固定7)/`drop_source`(int,经 `_DROP_SOURCE_MAP` 映射)。**meta key 已归一为普通 String 字面量**(`"monster_id"` 而非 `&"monster_id"`),与接收端 `has_meta("monster_id")` 字面对齐;Godot 4 内部 meta key 统一处理,String 写 / String 读 / StringName 读三种组合实测均命中。
- `elite_group=true` 的触发器在 `SpawnManager.wave_cleared` 时调 `LootManager.notify_elite_group_killed()` 喂软保底。
- `test_arena.tscn` 三波全配齐:Wave1(trash×5)/Wave2(elite_blue×8,elite_group)/Boss(butcher@7,elite_group)。

**meta key 两端逐一吻合**:战斗组 `set_meta` 的 `monster_id`/`monster_level` ↔ `ProgressionManager._on_enemy_killed` `get_meta`;`drop_source`/`monster_level` ↔ `LootManager._on_enemy_killed` `get_meta`。信号 `enemy_killed(enemy,killer,overkill,dir)` 签名一致。

**端到端验证** `tools/verify_combat_handoff.gd`(15 项全 `OK`):
- ProgressionManager 按 meta 查表给 XP:butcher@7=74 / trash@1=8(屠夫≠白怪,证明非兜底)。
- `drop_source` 枚举映射对齐:trash=0 / elite_blue=1 / champion_yellow=2 / butcher=3。
- 掉落行为:屠夫必掉(4件)、白怪概率掉落(373/2000≈18.6%)。
- meta 读写口径:String key 三条均可被接收端 `has_meta("...")` 读取;String 写 / StringName 读互通验证通过(印证战斗组归一不破坏任何读取路径)。
- 同批回归:`verify_progression`/`verify_drop_system`/`verify_data_tables`/`verify_item_gen` 四脚本全 `VERIFY OK`。

> **结论**:与战斗①的对接全部完成且验证通过。`Inventory.stats_changed`(装备生效)系统侧任务4 已落地;战斗① 已于 2026-06-13 在 `SkillExecutor` 接上该信号(`DamageCalculator.refresh_from_stats`),"换装变强" V2 端到端联通(`tools/verify_combat_equip_link.gd` 17 项全 `OK`,真实 `piercing_arrow.tres` × L8 策划参考体单发期望对齐)。无遗留未联通项。

### 6.4 Bug 修复:怪物死亡 `det == 0` 物理崩溃刷屏(2026-06-13)

**现象**:游戏运行中控制台每物理帧刷 `det == 0` 矩阵求逆错误(Godot 内部 Transform3D/Basis 反演失败)。

**根因**:怪物死亡走 `enemy_base.gd::_die()` → 用 tween 把本体 scale 缩到 `Vector3.ZERO`(DEATH_DURATION 后 queue_free)。但挂在同一 body 上的独立 `KnockbackComponent._physics_process` 仍在对这个**已退化(零缩放→基矩阵行列式为 0)**的 body 调 `move_and_slide()`,内部对退化基求逆即 `det == 0`,每帧刷屏。死亡与击退两条物理路径未互斥。

**修复**:
- **主修复** `scripts/entities/enemy_base.gd::_die()`:进入 `State.DEATH`、清零 velocity 后,若 `knockback_comp` 存在且有 `cancel()` 即调用,立即停止击退位移,scale 归零前击退已不再驱动物理。
- **防御兜底** `scripts/components/knockback_component.gd::_physics_process`:`_body == null` 守卫后追加 `_body.is_queued_for_deletion()` 判断,待销毁则 `cancel()` 并 `return`,杜绝对将销毁/退化 body 调 `move_and_slide()`。

**回归验证** `tools/verify_knockback_death.gd`(`VERIFY OK`,EXIT=0):
- 判定①:`apply` 后击退正常启动(is_active==true,初速/剩余时间已设)。
- 判定②(修复点):本体标记 `queue_free()` + scale→0 后,`_physics_process` 自动 `cancel()`,is_active 变 false 且后续帧保持停止,不再触发 `move_and_slide`。
- 注:SceneTree-script 测试 harness 中 `add_child` 不同步触发 `_ready`/无物理空间,故判定①以状态断言代替驱动真实 `move_and_slide`,避免空间缺失伪报。

### 6.5 扩展层落地:5 档稀有度 + 骸骨 P0 修正(2026-06-13)

依据 `数值表/扩展-稀有度-掉落-敌人差异化.md` §五 集成交接,落地装备稀有度从 3 档扩为 5 档,并修复骸骨卫士 P0 失衡。

**改动文件**:
- `scripts/data/item_instance.gd`:`Quality` 枚举 `{MAGIC,RARE,LEGENDARY}` → `{COMMON,MAGIC,RARE,EPIC,LEGENDARY}`(劣→优单调 0~4);`QUALITY_NAMES`/`QUALITY_COLORS` 补 普通(灰 `#9e9e9e`)/史诗(紫 `#a436ee`)。**全代码均按名访问 `Quality.XXX` / 字典查色,无硬编码整数依赖**,故重排安全(已 grep 全量核实:inventory/inventory_panel/loot_manager/loot_drop 全部按名)。
- `scripts/systems/item_generator.gd`:`AFFIX_COUNT` 补 `COMMON:[0,1]`/`EPIC:[4,5]`;`QUALITY_WEIGHTS` 改 5 元(55/33/10/1.5/0.5,源自 `drop_table.csv` 缓行走尸行);`generate()` 对 EPIC 走 `force_top`,`_roll_affixes(..., force_top)` 令**首条词缀取该 tier 顶值**(史诗保送 1 条顶值)。
- `scripts/data/affix_def.gd`:新增 `max_value(tier)`(返回 tier 范围上限,供史诗保送)。
- `scripts/systems/drop_system.gd`:`SOURCE_CONFIG` 6 源的 `w` 由 3 元 → **5 元 `[普通,精良,稀有,史诗,传说]`**,逐行对齐 `drop_table.csv`(TRASH/ELITE_BLUE/CHAMPION_YELLOW/BUTCHER/CHEST_COMMON/CHEST_FANCY);`_roll_quality` 改 5 档累加,梦魇系数+软保底(`pity_stack×2%`)仍只加到「传说」(index 4)权重。
- `data/monsters.tres` + `tools/gen_data_tables.gd`:`skeleton_guard health_mult 1.6→1.0`(扩展层 P0;tres 已用生成器重跑,默认值省略=1.0)。

**回归验证**(headless,Godot 4.6.3):
- `tools/verify_item_gen.gd` `VERIFY OK`:5 档权重抽样 55.5/32.4/10.2/1.5/0.5(期望 55/33/10/1.5/0.5);史诗 200/200 件首条命中顶值;普通 0~1 词缀;靴子部位约束 0 非法。
- `tools/verify_drop_system.gd` `VERIFY OK`:精英 5 档分布 10/46/34/7/3(期望 ~10/48/33/6/3);TRASH 掉率 18.4%;首橙白名单/硬保底/前4件查重/满包全过。
- `tools/verify_inventory_equip.gd` `VERIFY OK`:枚举重排后装备/卸下/属性聚合/双戒/换装全过(证明跨消费者无回归)。

> **opt-in 口径**:demo 主线与 5 档共用同一掉落表;普通/史诗按权重自然出现,无需开关。传奇获取主通道仍走 `loot_timeline` 软/硬保底(本次不动)。敌人差异化(弓手远程/肿胀自爆/召唤者)属战斗① 域消费 `enemy_types.csv`,本次未涉及。

### 6.6 基底/史诗物品表落地 + 五档程序化命名(2026-06-13)

依据 `数值表/掉落物清单-说明.md`(§一数量设计/§二基底表/§三命名规则/§四史诗保送自检)与 `数值表/扩展-稀有度-掉落-敌人差异化.md` §五集成交接,把"物品长什么样、叫什么名"从代码硬编码落成数据表 `.tres`,并实装五档程序化命名。

**配置表核对(零差异,未改 CSV)**:`affixes.csv`(10 词缀)/`affix_naming.csv`(逐词缀前缀/后缀/稀有名素材)/`base_items.csv`(12部位×3Tier)/`epic_items.csv`(10 件具名史诗+保送词缀)四表均与两份说明文档逐项一致,经人工审计 + 子代理交叉复核四方一致,无需修改。

**新增配置表** `数值表/rarity_naming.csv`(+`.csv.import` 已设 `importer="keep"`):独立沉淀 §三五档命名模板(普通=基底名 / 精良=前缀+基底[+后缀若≥2词缀] / 稀有=两段稀有名素材·连接+基底 / 史诗+传说=固定具名),供策划查阅,非运行时读取。

**新增 4 个 Resource 类**(强类型 schema):
- `scripts/data/item_base_def.gd`(`class_name ItemBaseDef`):部位 / 三档基底名 / `base_value_kind`(武器DPS·防具·无)/ `main_roll_affixes`,`name_for_tier(tier)` 取档名。
- `scripts/data/base_item_table.gd`(`class_name BaseItemTable`):`base_items: Array[ItemBaseDef]`。
- `scripts/data/epic_item_def.gd`(`class_name EpicItemDef`):id / 具名 / 部位 / `guaranteed_affix_id`(保送词缀)/ 副词缀提示 / build 倾向。
- `scripts/data/epic_item_table.gd`(`class_name EpicItemTable`):`epics: Array[EpicItemDef]`。
- **注**:新增 `class_name` 不会被 `--script` 直接识别,生成器首跑前须先 `godot --headless --path . --import` 重扫全局类缓存(同战斗① 警告的 error 43 race),否则 `Could not find type` 解析失败。

**生成器** `tools/gen_data_tables.gd`:新增 `_build_base_items`(镜像 `base_items.csv` 12部位×3Tier)+ `_build_epics`(10 件具名,`guaranteed_affix_id` 映射到 `affixes` id),`_init` 追加两条 `_save` 出 `data/base_items.tres` + `data/epic_items.tres`(GEN_EXIT=0,内容已核对)。

**加载器** `scripts/autoload/data_tables.gd`:新增 `_load_base_items()`/`_load_epics()`(`_load_all` 调用)+ 查询 API:`get_all_base_items` / `get_base_item(slot)` / `get_base_name(slot, tier)` / `get_all_epics` / `get_epic(id)` / `get_epics_for_slot(slot)`。

**生成器消费** `scripts/systems/item_generator.gd`:
- `const NAMING`:10 词缀的前缀/后缀/稀有名素材查表(对齐 `affix_naming.csv`)。
- EPIC 路径改为 `_pick_epic_for_slot(slot)` 选具名史诗,强制其 `guaranteed_affix_id` 作保送词缀(无对应史诗的部位回退 `&"__TOP__"`)。
- `_roll_affixes(slot, tier, count, guaranteed_id)`:保送/`__TOP__` 词缀移到首位并以 `max_value(tier)` 顶值 roll(史诗保送 1 条顶值,与 §四自检对齐)。
- `_make_name(quality, slot, affixes, tier)`:普通=基底名 / 精良=前缀+基底(≥2词缀追后缀)/ 稀有=两段稀有名素材·连接+基底;史诗+传奇走固定具名。`generate_legendary` 经默认参数兼容,无回归。

**回归验证**(headless,Godot 4.6.3):
- `tools/verify_item_gen.gd` `VERIFY OK`:史诗 200/200 件保送词缀命中顶值;普通名=纯基底(如「皮腰带」);稀有名=两段素材(如「龟甲·熊 链胸甲」);五档权重抽样 55.5/32.4/10.2/1.5/0.5。
- `tools/verify_inventory_equip.gd` `VERIFY OK`:DataTables 新增加载路径后背包/装备/属性聚合零回归。

> **单一事实源**:基底/史诗"长什么样·叫什么"现由 `数值表/{base_items,epic_items,affix_naming,rarity_naming}.csv` 定义,经 `gen_data_tables.gd` 镜像进 `.tres`;数值词缀范围仍由 `affixes.csv` 定。换表后重跑生成器(先 `--import` 再跑)即可,接口不变。

---

## 七、V3.0 单局大秘境固定数值版 · 系统② Day2（2026-06-14）

依据 `数值表/大秘境-单局固定数值与关卡配置.md` §七任务分配，系统②（程序②）负责的 5 条已全部落地并 headless 验证通过。玩法范式由"关卡成长+概率掉落"改为"单局大秘境·固定数值"：取消等级成长/经济/梦魇/概率掉落；仅大秘境 L2；小怪零掉；守门人（复用屠夫）死→固定爆装（仅橙+绿套装）。

> **域边界**：点地移动/D3 键位、锁玩家固定战斗面板/禁装备改战斗数值、怪物固定值（接 `rift_monsters.csv`）属**战斗①**；地图清理（LANDMARKS 去传送门/宝箱）+ ENCOUNTERS 接固定怪属**关卡C**；光柱 VFX 属**美术（角色D）**。系统②只动掉落/品质/流程/展示面板/进度逻辑。

### 7.1 ② 仅橙/绿品质 + `is_set` 绿光柱（P0-2）

- `scripts/data/item_instance.gd`：新增 `@export var is_set: bool = false` + `const SET_COLOR = Color(0.13,0.85,0.18)` + `display_color()`（**套装绿优先级 > 品质橙**）。**不新增 Quality 枚举档**，绿套装 `quality` 仍 = `LEGENDARY`、靠 `is_set` 视觉旁路，与已落地 5 档枚举零冲突。
- `scripts/entities/loot_drop.gd`：`_apply_visuals()` 改用 `item.display_color()`；光柱对 `is_legendary() or is_set` 可见。

### 7.2 ① 掉落重构：小怪零掉 + 守门人固定爆装（P0-1，读 `boss_drop_list.csv`）

- `scripts/autoload/data_tables.gd`：新增 `BOSS_DROP_LIST_PATH` + `_load_boss_drops()`（`FileAccess.get_csv_line` 直读，跳末尾"约束"行）+ `_SLOT_ZH_MAP`（中文部位→`EquipSlots.Slot`）+ **`get_boss_drop_items() -> Array[ItemInstance]`**（每次构造全新 14 件，`quality=LEGENDARY`，第二枚戒指落 `RING_2`）+ `get_boss_drop_count()`。**直接读 CSV 作单一事实源**（`legendaries.tres` 仅 5 件不足 14 件，用户授权直读）。
- `scripts/systems/drop_system.gd`：`SOURCE_CONFIG` 把 TRASH/ELITE_BLUE/CHAMPION_YELLOW/CHEST_* 的 `drop_rate→0.0`（小怪全程零掉）；`roll_drop()` 对 `Source.BUTCHER` 短路到新增的 `boss_drop()`（返回全部 14 件固定爆装）。守门人**复用 `butcher`（drop_source=3）枚举**，无需新增源。
- `scripts/autoload/loot_manager.gd`：`_spawn_loot()` 按掉落件数放大散布半径（14 件不光柱重叠）；`drop_source=3` 已透明路由，触发逻辑无需改。
- 验证 `tools/verify_boss_drop.gd`（6/6）：14 件 / 2 绿套装 / 全 LEGENDARY / 绿橙 display_color 旁路 / 双戒分 RING_1·RING_2 / 小怪 500×3 源零掉 + 守门人爆 14。

### 7.3 ③ 移除 L1 场景流程，直进 L2（P0-3）

- `project.godot`：`run/main_scene` 由 `scenes/main.tscn` → **`scenes/levels/level_02_play.tscn`**（启动直进 L2）。
- **删除** L1 资源：`scenes/levels/level_01_{gate,play}.tscn` / `scripts/levels/level_01_gate.gd`(+`.uid`) / `tools/verify_level01.gd`(+`.uid`)。（经核实仅被自身+verify 工具引用，无玩法代码依赖。）
- `scripts/components/level_exit.gd`：清掉 L1 残留默认值（`level_id`/`next_hint` 默认改 L2）。
- 验证 `tools/verify_l2_boot.gd`（3/3）：L2 启动场景可加载+可实例化（依赖链完整）+ L1 场景文件已移除。
- **域边界**：仅动流程+启动入口；L2 地图布局/LANDMARKS/ENCOUNTERS 属关卡C，未动。

### 7.4 ④ 装备面板数字写死中期档·换装不改数值（P1-1，展示侧）

- `scripts/autoload/data_tables.gd`：新增 `PLAYER_LOADOUT_PATH` + `_load_player_loadout()`（读 `player_loadout.csv` key/value）+ **`get_fixed_panel_stats() -> Dictionary`**（StatKind→固定值，源 loadout CSV：敏捷150/暴击45/暴伤400/攻速1.5/武器24/生命1200，缺项 build 兜底）。
- `scripts/ui/inventory_panel.gd`：`_refresh_all()` 改取 `DataTables.get_fixed_panel_stats()`；**断开 `stats_changed` 重算**，换装只刷背包/装备槽视觉，面板数字恒定（拾取仅观感）。
- **域边界**：本条是**展示面板**写死（系统②）；战斗数值锁面板/禁装备改战斗属性属战斗①。⚠️ 此举**反转**了 V2"换装变强"的展示联动（战斗侧 `DamageCalculator` 未动，由战斗① V3 处理）。
- 验证 `tools/verify_fixed_panel.gd`（4/4）：loadout 解析 / 10 属性 / 关键值对齐 loadout / 多次取值恒定。

### 7.5 ⑤ 进度条 + 守门人触发（P1-2，RiftManager）

- **新增 autoload** `scripts/autoload/rift_manager.gd`（注册于 `project.godot`，置于 CombatManager 之后）：监听 `CombatManager.enemy_killed`，据 `enemy.get_meta("monster_id")` 查权重累计进度（白怪+1.0 / 蓝名+5.0 / 黄名+8.0 / 时间球+3.0；守门人不计）。`GOAL=106`，每实例只计一次（防重复）。满 GOAL→发 `guardian_ready` + `call_deferred` 切 `boss_room_play.tscn`。对外：`signal progress_changed(value,goal)` / `signal guardian_ready()` / `add_time_ball()` / `reset_rift()`。
- `scripts/ui/hud.gd`：顶部中央新增大秘境进度条（紫，跟现有 XP 条同 ProgressBar 风格），连 `RiftManager.progress_changed` 实时刷百分比，满显示"守门人降临!"。
- **域边界**：`add_time_ball()` 仅留接口，时间球实体的放置属关卡C/美术；切场目标 `boss_room_play.tscn` 已有。
- 验证 `tools/verify_rift_progress.gd`（6/6）：1+5+8 权重 / 同怪只计一次 / 守门人不计 / 时间球+3 / 满106 触发 guardian_ready / 满后锁定。
- 全工程 headless 启动（`--quit-after`）：直进 L2 + 全 autoload（含 RiftManager）+ AnimLib 干净加载，无报错。

> **对外接口新增（队友看）**：
> - 战斗①/关卡C：怪挂 `monster_id` 元数据（已有口径）即自动喂进度；精英用 `elite_blue`/`champion_yellow`。时间球拾取调 `RiftManager.add_time_ball()`。重开一局调 `RiftManager.reset_rift()`。
> - 美术（角色D）：守门人爆装观感=14 件满地光柱（橙优先 `is_set` 绿，`loot_drop` 已按 `display_color()` 上色）；进度条 HUD 为占位紫条，可后期重皮。
> - 守门人=屠夫复用 `drop_source=3`;其死亡=单局通关,不喂进度权重。

### 7.6 传奇 retheme:女武神之冠 → 箭舞之冠（2026-06-14）

- `tools/gen_data_tables.gd`：`_build_legendaries()` 第 4 件由 `valkyrie_crown`/「女武神之冠」/`valkyrie_plus1`/「女武神召唤数+1」**改为** `arrow_dance_crown`/「箭舞之冠」/`rain_extra_volley`/「箭雨风暴额外多射 1 轮箭矢且范围 +20%」，对齐单一事实源 `legendaries.csv` 与 `boss_drop_list.csv`#4。
- 重跑生成器（先 `--import` 再 `--script`）→ `data/legendaries.tres` 已更新（id `arrow_dance_crown` / 「箭舞之冠」）；全仓 grep 无 `valkyrie_crown`/`valkyrie_plus1` 残留。
- **域边界**：召唤随从「女武神」（`scripts/entities/valkyrie.gd` / `skill_valkyrie` / `valkyrie_summon.tres`）是战斗①的另一实体，与本传奇头盔同名巧合，**未改动**。

### 7.7 弃用 .tres 清理 = 阻塞项（2026-06-14 核实，暂缓）

任务清单② line 51「清理弃用 `monsters/xp_curve/tier_table/base_items/epic_items.tres`」经代码核实**前提不成立**，记录如下供后续跨域决策：

- **`monsters.tres` + `xp_curve.tres` 仍 load-bearing**：`ProgressionManager` 仍是 live autoload（`project.godot:26`）。V3.0「取消等级成长」只落在**设计文档**，**代码层 XP/升级系统未拆**：击杀给 XP 仍调 `DataTables.get_monster_stats(id, level)`；XP 阈值 / 属性成长（`agility_at/vitality_at/max_hp_at`）/ 技能解锁 payload（`unlocks_at`）/ HUD 经验条全依赖 `xp_curve`。删表 = 破坏 ProgressionManager + HUD。
- **`tier_table/base_items/epic_items.tres` 条件性**：仅被 `ItemGenerator.generate()` 随机路径消费（`_tier_for` / `get_base_name` / `get_epics_for_slot`）。`ItemGenerator` 非 autoload，但仍经 `LootManager._ready → DropSystem.new(dt, gen)` 实例化（小怪掉率已置 0，**构造链未拆**）。删表 = `DropSystem` 构造链 + 7 个 verify 工具报错（verify_data_tables / progression / item_gen / drop_system / boss_drop / combat_handoff + gen_data_tables）。
- **真正前置 = 跨域决策**：本条等价于「V3.0 是否**物理移除**等级成长系统（拆 `ProgressionManager`）+ 随机生成系统（拆 `ItemGenerator`）」，属战斗①+系统② 共同拍板，非系统② 单独 0.5h 清理。**当前这些表不报错、不影响玩法，删除零运行时收益**，故标记阻塞、暂缓，待跨域拍板后再评估。

### 7.8 守门人死亡结算面板 UI（2026-06-14，通关页 line 49 / line 37）

守门人（=屠夫）死亡 = 单局大秘境通关，弹结算面板。全部落在系统②域内，零侵入战斗①/关卡C/美术D。

- **`scripts/autoload/rift_manager.gd` 增强**（仅加字段/信号/访问器，不改既有权重逻辑）：
  - 新增 `signal run_cleared(clear_time_sec: float, kill_count: int)`——守门人死亡时发出。
  - 新增 `var run_start_ms`（`_ready` 启动即记 / `reset_rift` 归零）、`var kill_count`（每次有效 `enemy_killed` +1，含小怪/精英/守门人 = 击杀总数）。
  - `_on_enemy_killed`：守门人（`monster_id == "butcher" or "guardian"`）死亡 → emit `run_cleared(get_clear_time(), kill_count)` 且**不喂进度**（替换原 `w<=0` 分支，语义不变）。
  - 新增访问器 `get_kill_count() -> int` / `get_clear_time() -> float`。
- **新建 `scripts/ui/settlement_panel.gd` + `scenes/ui/settlement_panel.tscn`**：CanvasLayer `layer=120`（盖 HUD/背包之上），全程序化 UI，与 `inventory_panel.gd` 同风格。监听 `RiftManager.run_cleared` 弹出（淡入 Tween）：金色大标题「大秘境通关！」/「用时 mm:ss 击杀 N」/ 14 件固定掉落清单（用 `ItemInstance.display_color()` 自动处理橙/绿套色，绿优先）/「按 R 重新开始」。R 键 → `RiftManager.reset_rift()` 后 `change_scene_to_file` 切回 `level_02_play.tscn`。
- **`scenes/levels/boss_room_play.tscn` 补挂 UI**：此前 boss 房**完全无 UI**，现实例挂 `HUD`(100)+`InventoryPanel`(110)+`SettlementPanel`(120)，与 L2 体验一致（血条/进度条/背包齐全 + 通关结算）。
- **已核验前提**：屠夫**仅**在 boss 房经 `boss_room.gd` 触发生成，L2 全程不生成 → 「butcher 死 = 通关」安全（监听 `enemy_killed` 不会误触发）。
- 验证 `tools/verify_settlement.gd`（5/5）：run_cleared 信号 + 访问器齐全 / reset 后计数累加 + 用时≥0 / 守门人死触发 run_cleared 不喂进度 / settlement_panel.gd 解析 OK / boss 掉落 = 14 件。boss 房场景 headless 加载 EXIT=0 无报错。
- **运行树集成校验** `tools/settlement_ui_harness.tscn`（16/16，作临时主场景跑·autoloads 全活）：真实挂面板 → 真实 `RiftManager.run_cleared.emit` → 断言面板 visible / 用时(02:05)·击杀(37) 文案 / 14 行掉落全上色 / 2 行绿套色 / 重复 emit 幂等。比纯解析更进一步，验证真实信号接线 + UI 渲染。
- **端到端主流程校验** `tools/verify_boss_scene_settlement.tscn`（10/10）：直接实例化 `boss_room_play.tscn`（=RiftManager 满进度 `change_scene` 的目标场景）→ 断言其子树挂有 HUD/InventoryPanel/**SettlementPanel** → 真实 emit `run_cleared(212,53)` → 面板显示 + 14 行掉落 + 用时 03:32/击杀 53。确认面板已并入唯一会触发它的场景（守门人整场景切到 boss 房后死亡）。

> **对外接口新增（队友看）**：
> - 关卡C / 流程：重开一局走 `RiftManager.reset_rift()`（已清进度+计时+计数）；通关页 R 键已实现切回 L2，如需改通关后流程改 `settlement_panel.gd::START_SCENE`。
> - 美术（角色D）：结算面板为占位程序化 UI（金标题 + 品质色文字列表），可后期重皮；14 件掉落文案/品质色读 `DataTables.get_boss_drop_items()`。

### 7.9 速通测试 override 机制（2026-06-14，`数值表/测试-1分钟速通`）

按 `数值表/测试-1分钟速通/README-1分钟速通测试.md` 规范实现「测试开关 → 读 `speedrun_test.csv` → 运行期套用差量」。**红线遵守**：override 不是新玩法只压时长；**不进正式包**（不带开关 = 走正式值，零污染）；单一事实源仍是正式 CSV + RiftManager，本表只列差量。

- **开关**：命令行 `--speedrun`（`OS.get_cmdline_user_args().has("--speedrun")`）。不带 = 完全走正式值。
- **唯一改动文件 `scripts/autoload/rift_manager.gd`**（零侵入战斗①/关卡C/美术D）：
  - `const GOAL=106.0` **保留不动**（外部 `hud.gd` 首帧兜底 / `verify_rift_progress` 断言仍读它）；新增 `var goal=GOAL` 作运行期有效目标，内部进度逻辑 + `progress_changed` 信号一律用 `goal` → HUD 经信号自动跟随（速通时 goal=15）。
  - `_load_speedrun_overrides()`（`_ready` 调）：仅 `--speedrun` 时读 `speedrun_test.csv`（FileAccess 直读·importer=keep）；纯套用逻辑抽成 `_apply_overrides(ov)` 供 verify 直测。`启用!=1` → 忽略。
  - **进度条目标 15**：`goal = 15`（核心知识点，系统②域）。
  - **守门人 HP 4000**：系统② **不改 `butcher.gd` 的 const**；速通时连 `get_tree().node_added`，守门人进场（`monster_id` meta == `butcher`/`guardian`，与击杀判定同口径）→ 延一帧（meta 在子节点 `_ready` 才 set）→ set 其实例 `current_health = 4000`。零侵入战斗①。
- **未实现/N-A（按 README 允许）**：守门人 ATK（const 用在 3 处，README 明示「可选·留 90 亦可」→ 留正式 90）；时间条 120s（RiftManager 当前**无限时失败机制**，无对应字段 → N/A）；刷怪密度/玩家DPS/精英HP 倍率（全 = 1.0 不动，且分属关卡C/战斗①）；跳过选层（V3.0 已直接 boot 到 L2 无选层 UI → 天然满足）；守门人固定掉落（沿用 `boss_drop_list.csv` 不变）。
- 验证 `tools/verify_speedrun.gd`（8/8）：① 默认零污染（speedrun=false·goal=106）；② override 套用（goal=15·hp=4000·speedrun=true）；③ 启用=0 忽略；④ 缺字段回落默认；⑤ goal=15 喂 15 白怪即满+触发 guardian；⑥/⑥b 守门人 HP 进场覆写 4000·非守门人不动；⑦ CSV 字段核对。
- 回归:`verify_rift_progress` 仍 6/6（GOAL→goal 重构无破坏）；`--headless -- --speedrun` 实机 boot 打印「速通模式生效: goal=15 守门人HP=4000」无脚本错误。
- **一键启动** `run_speedrun.bat`（仓库根）：`Godot --path . -- --speedrun`（注意 `--` 后才进 `OS.get_cmdline_user_args()`）。Godot 路径用 `%~dp0..`（本机 Godot 在 Downloads = 项目上一级)相对解析,缺失时打印提示。队友环境不同需自行改脚本里 `GODOT` 路径。

### 7.10 修复:boss 房生成占位走尸而非真屠夫 → 不掉装备/不弹结算（2026-06-14）

**现象**：大秘境进度满切到 boss 房，打死「boss」后既不掉装备、也不弹结算页。速通模式下守门人 HP 覆写也不生效。

**根因**（`scripts/levels/boss_room.gd::_build_boss`）：boss 房一直用 `SpawnTrigger` 生成的是**占位走尸**（`enemy_zombie.tscn` + `walking_corpse.tres`），其 `monster_id` 不是 `butcher`/`guardian`。导致三条链全断在源头：
- RiftManager `_on_enemy_killed` 判 `mid != butcher` → **不 emit `run_cleared`** → 结算页不弹（§7.8）。
- LootManager 读 `drop_source` = TRASH（非 BUTCHER）→ 走 `roll_drop` 普通分支，而大秘境小怪 `drop_rate=0` → **不掉装备**（§7.2）。
- 速通 `_try_apply_guardian_hp` 找 `monster_id==butcher` → 匹配不上 → **HP 覆写无效**（§7.9）。

**修复**：`_build_boss()` 改为直接实例化真屠夫 `scenes/enemies/butcher.tscn` 到 `BossSpawn` 位置（弃用占位走尸的 SpawnTrigger 方案）。`butcher.tscn` 自带 `butcher.gd`，`_ready` 设 `monster_id=butcher` + `drop_source=3`(BUTCHER) meta → 三条链全部对上。零侵入战斗①/掉落/结算既有逻辑。

**实机验证**（`--speedrun` 实跑打死屠夫，日志全通后已清临时调试 print）：
- `enemy_killed: mid=butcher has_drop_source=true` ✓
- LootManager `boss_drop` → roll 出 **14 件** ✓
- RiftManager → emit `run_cleared(t=11.0, kills=6)` ✓
- SettlementPanel `_on_run_cleared` 弹出 ✓

**附带**：速通 `数值表/测试-1分钟速通/speedrun_test.csv` 由 1 分钟档调成 **~20s 体验档**(进度条目标 15→5 / 守门人HP 4000→1500)，`tools/verify_speedrun.gd` 测⑦ 同步改成核对 5/1500（8/8 仍通过）。

### 7.11 功能塔（伤害 buff 塔 / 加速塔）策划定案（2026-06-14，待实现）

新增核心回路机制，已写入 `分工/Demo-Gamejam-2天4人-重切方案_v2.md` §2.5，归属**系统(程序②)**：
- **伤害塔**：互动激活 → 全局伤害 **+30%**，持续 **8s**，CD **20s**。
- **加速塔**：互动激活 → 全局移速 **+35%**，持续 **8s**，CD **20s**。
- **互斥**：同时只生效一个 buff，激活第二座替换第一座。
- 定位：正式纳入核心回路（非 stretch）。重切方案 §3 流程 / §4 分工 / §6 砍·加阶梯 / §8.1 职责域已同步交叉更新。

> 实现待办：塔交互节点 + 全局 buff 管理（计时/CD/互斥）→ 接入战斗伤害与移速属性聚合。
