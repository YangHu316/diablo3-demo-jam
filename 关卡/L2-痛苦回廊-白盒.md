# L2 ·「痛苦回廊」白盒设计(据手绘平面图搭建)

> 关卡C(策划) / 2026-06-13 / 大型地牢白盒 · 白盒测试用
> 落地文件:`scenes/levels/level_02_depths.tscn`(关卡)、`level_02_play.tscn`(可玩包装·F6)、`scripts/levels/level_02_depths.gd`(程序几何 + 栅格 navmesh 编译器)
> headless 校验:`tools/verify_level02.gd`(连通/路径长度/通关时长)

---

## 一、对原平面图的解读

把提供的手绘地牢平面图(散开、多分支、有环路)解读为**轴对齐近似拓扑**(白盒标准做法:对角走廊折直、保拓扑不抠像素)。原图招牌要素 → 本关落点:

| 原图要素 | 本关 |
|---|---|
| 两个蓝色传送门/水晶 | **西门**(入口·绿门)+ **北门**(目标·蓝门,远北纵深尽头) |
| 中央十字枢纽 | 中央枢纽 H(西/北/东/南四向分叉) |
| 很长的北向走廊 | **N1 长北廊**(纵深主轴,串火光) |
| 上中部带分隔房间 | 上中室 UC |
| 齿轮(机关)节点 | 齿轮室 GR(清场=机关转开的读感) |
| 右侧矩形走廊围成的环路 | **右环 RL_N/E/S/W**(绕中心实体的回游) |
| 右下角带中心圆形特征的大房 | **Boss 厅**(中心柱 + 环形台座,高潮) |
| 左下角死胡同小圆室 | **宝藏死胡同 TR** |
| 下部连回枢纽的大环路 | **下环 S1→S2**(枢纽南→汇入 Boss 入口) |

---

## 二、技术:rect→栅格 navmesh 编译器 + 自动墙(为什么这么做)

大关卡(18 房间/走廊)若手编 navmesh 需 200+ 共享顶点多边形,极易断。本关用**程序化编译器**(`level_02_depths.gd`):

1. 关卡 = 一组**可走矩形 `WALK`**(房间+走廊,相邻处重叠)。
2. 把所有矩形的 X/Z 边界并成一张**栅格**;每个落在任意矩形内的栅格单元 = 一个 navmesh 四边形,**顶点按栅格坐标去重共享** → 相邻单元天然共边连通(连通性由构造保证,实测 224 面全连通)。
3. **墙体由同一栅格自动生成**:可走单元与「不可走邻格」的边界 = 一段墙。开口恰在矩形相接处。
4. 全部几何 `collision_layer=4`(玩家 mask=4 / 敌人 mask=5 与之碰撞)。

> 改关卡 = 改 `WALK` 数组(再跑 `verify_level02.gd`)。`SCALE` 常量统一缩放(当前 1.5,放大避免局促、拉长纵深)。
> 备注:运行时烘焙(bake)在 headless 下 map 同步不稳;手编共享顶点 navmesh 实测稳定,故采用此法。

---

## 三、核心路径、区域与节奏

```
西门(入口) → 西廊[首遇] → 中央枢纽 ┬─北→ 长北廊[火光·伏击] → {上中室; 北门(目标·光柱牵引)[守卫]}
                                   ├─西南→ 宝藏死胡同
                                   ├─东→ 东廊[过渡] → 齿轮室[机关] → 右环(绕中心实体)[尸群] → Boss入口廊 → Boss厅(中心柱)[尸潮高潮·出口]
                                   └─南→ 枢纽南廊 → 南长廊[下环] ──→ 汇入 Boss 入口(与东路构成大环路)
```

- **入口/出口**:入口=西门;出口=Boss 厅深处(击杀高潮后出关)。**目标**=北门(远北,纵深主轴尽头,光柱远端可见)。
- **环路**(原图招牌):① 右环 RL 绕中心实体回游;② 下环 S1→S2 与东路两条到 Boss(实测 东路 130 / 下环 116,长度相近,互为替代——"绕路不亏"的好环)。
- **区域类型**:战斗(枢纽/东廊/齿轮室/右环/南长廊/北廊/北门/Boss入口/Boss厅);纵深廊(N1 北廊串火光);资源/支线(宝藏死胡同、北门目标);休整靠节奏谷(廊段)与篝火光点。

---

## 四、遭遇与难度曲线(11 处,复用走尸 + SpawnManager)

前置开场(西廊3)→ 枢纽4 → 过渡4 → 东廊4 → 齿轮5 → 右环6 → 南长廊5 → 北廊5 → 北门4 → Boss入口6 → **Boss厅12(环绕高潮)**。合计 ≈58 只,峰值并发 ≤12(各波空间分离、顺序清场)。
- 东路主线数量**单调爬升不回落**,Boss 厅 12 环绕作份量足的高潮。
- **窄廊只用 cluster/line**;`surround` 仅给开阔 Boss 厅(避免 surround 半径超走廊半宽把怪刷进墙——评审查出的破面已修)。

---

## 五、规模 vs 通关时长(实测 + 论证)

| 度量 | 值 | 口径 |
|---|---|---|
| navmesh | 224 面,全连通,无 softlock | headless 寻路实测 |
| 直达临界路径(入口→Boss) | **292 单位 ≈ 42s 步行** | 东路 |
| 设计路线(入口→北门目标→Boss) | **513 单位 ≈ 73s 步行** | 取北门纵深主线 |
| 估单程通关 | **直达 ≈4min / 设计路线 ≈5–6min** | 11 遭遇(≈58 只)战斗占大头 |
| footprint(×1.5 后) | ≈ 265×255 单位 | 比 L1(≈55)大一个量级,纵深充足 |

走廊净宽 12–18u(战斗廊加宽留弓系风筝空间)、房间 24–51u(开阔不局促);墙高 2.4 减俯视近侧遮挡。

---

## 六、多智能体评审与已应用修复(透明记录)

跑了 5 镜头(节奏/空间纵深/遭遇曲线/softlock/对图忠实度)+ 综合的设计评审。综合判定:**部分达成**(拓扑忠实、全连通、规模对 5–6min 合理;弱点=可抄近路 + 长廊"纸面纵深" + 一处 surround 破面)。**已应用的低成本修复**:

- ✅ 修 surround 破面:右环·南 `surround r7`→`cluster r4`(贴合窄廊半宽)。
- ✅ 难度曲线单调:东廊 3→4、Boss 入口 4→6;Boss 厅 8→12 高潮。
- ✅ 填空走段:前置西廊首遇(开场不冷场)+ 枢纽→东过渡波(消 26s 空走)。
- ✅ 纵深可读:N1/S2 长廊每约 18–22u 串暖光火盆(把看不见的纵深换成一段段可见推进);北门加**高光柱 + 强光(range 28)**远端牵引。
- ✅ 不局促:东廊/右环南/南长廊加宽到 12u;墙高降到 2.4。

> 评审硬建议「**白盒不再加新机制**」,故未做 kill-gate;详见下"已知取舍"。

---

## 七、白盒已知取舍 & 待办

- **抄近路 / 目标非强制**:无机关门逻辑,玩家可走东路直达 Boss(292u)跳过北门目标。这是回环型地牢在无 gate 时的固有性质。**强制纵深主线(清北门守卫→开门通 Boss)留作 Day2**:复用 `SpawnManager.wave_cleared` + 一段可移除墙即可,零新系统;白盒先保可玩落地。
- **地标碰撞未在 navmesh 抠洞**:中心柱/门柱等密集处怪可能贴住,美术替模时由烘焙处理。
- **待我(Day2)**:罐桶/血球资源点、教学/目标 Toast 文案、首装脚本掉落接系统② 掉落入口、按 playtest 调遭遇数量与火光位、(可选)北门 kill-gate。

---

## 八、怎么试玩 + 校验结果

- **试玩**:Godot 打开 `scenes/levels/level_02_play.tscn` 按 **F6**(玩家自动落在西门入口,相机俯视跟随)。**不改 `main.tscn`**(战斗① 在用)。
- **重跑校验**:`Godot_v4.6.3 --headless --path . --script res://tools/verify_level02.gd`
  当前:navmesh 224 面 ✓ / 全路由连通无 softlock ✓ / 设计路线 513u ✓ / 两条环路皆通 ✓。
- 俯视布局图见对话里的 `l2_depths_blockout` 可视化。
